Task 2 - web service and client for it
    written in ruby(soap4r), 
    using YAML to store data, 
    using gtk2 for client front end

web service - personal movies collection
    operations:
        getMovies
        rateMovie
        addMovie
